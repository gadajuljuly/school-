{
  "name": "itask-android",
  "private": true,
  "version": "1.0.0",
  "description": "Native Android wrapper for ITASK (loads the live site over server.url, adds the native Contacts plugin the TWA build can't use)",
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor-community/contacts": "^7.0.0",
    "@capacitor/push-notifications": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
